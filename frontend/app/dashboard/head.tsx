export default function Head({ params }: { params: { slug: string } }) {
    // You can use params in this way if your route has params
    return (
      <>
        <title>Dashboard</title>
      </>
    );
  }